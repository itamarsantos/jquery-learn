$(function(){
    // $('.filha').parent(); Pega div que o contém
    // $('body').find('div').eq(0); // Encontra primeira div dentro de body
    // $('conteudo').parent(); Pega div que o contém, cada parent sobe uma div
    // $('.irmao').closest('.topo'); Closest procura elemento pai mais próximo
    // $('.irmao').closest('.topo').find('.conteudo'); Voltar para div geral para pesquisar elementou de outro bloco
    // Só utilizar parent caso saiba que o elemento será aquele
});
